[project]
name = "bpmn-modeling"
version = "1.1.0"
description = "BPMN modeling skill for process automation"
requires-python = ">=3.10"
dependencies = [
    "bpmn-lib>=4.0.5",
]

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.backends._legacy:_Backend"
